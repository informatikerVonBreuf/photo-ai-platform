/**
 * Guide d'installation des dépendances pour la scène 3D
 * 
 * Exécutez cette commande dans le terminal :
 */

npm install three @react-three/fiber @react-three/drei

/**
 * Dépendances installées :
 * - three : Moteur 3D JavaScript
 * - @react-three/fiber : Wrapper React pour Three.js
 * - @react-three/drei : Utilitaires et composants prêts à l'emploi (Float, IcosahedronGeometry, etc.)
 * 
 * Versions recommandées (compatible avec React 18+) :
 * - three@latest
 * - @react-three/fiber@latest
 * - @react-three/drei@latest
 */
